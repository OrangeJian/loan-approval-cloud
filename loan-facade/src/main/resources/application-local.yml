server:
  port: 8081

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.apache.rocketmq.spring.RocketMQAutoConfiguration
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false

rocketmq:
  enabled: false

mybatis-plus:
  enabled: false

local:
  mock:
    enabled: true
