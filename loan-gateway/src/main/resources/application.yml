server:
  port: 8080

spring:
  application:
    name: loan-gateway
  redis:
    host: localhost
    port: 6379

cloud:
  nacos:
    discovery:
      server-addr: localhost:8848
    config:
      server-addr: localhost:8848

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 10000
