server:
  port: 8087

spring:
  application:
    name: loan-pay
  redis:
    host: localhost
    port: 6379

cloud:
  nacos:
    discovery:
      server-addr: localhost:8848
    config:
      server-addr: localhost:8848

rocketmq:
  name-server: localhost:9876
  consumer:
    group: loan-pay-consumer-group
